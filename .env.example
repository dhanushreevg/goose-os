# GOOSE OS — environment template.
# Copy to .env and adjust locally. Never commit real secrets.

# Local system-service bridge (read-only).
GOOSE_SERVICE_HOST=127.0.0.1
GOOSE_SERVICE_PORT=4823

# Set to 1 to serve bundled demo data instead of reading the host machine.
GOOSE_SERVICE_DEMO=0