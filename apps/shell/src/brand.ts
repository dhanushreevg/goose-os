/**
 * GOOSE OS branding constants.
 *
 * The official logo lives in the shell's `public/` directory so Vite serves
 * it at a stable URL in both dev and production builds.
 */
export const GOOSE_LOGO_URL = '/brand/goose.jpeg';